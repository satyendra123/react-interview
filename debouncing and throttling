Throattling- means agar ek button diya hai to ek bar click hone ke bad agar mai continuously bhi click karte jaau to wo har 5 second ke bad hi click ho aur count update ho

EX-1
import React, { useRef, useState } from 'react';
import './style.css';

const App = () => {
  const [count, setCount] = useState(0);
  const buttonRef = useRef(false);

  const handleClick = () => {
    if (buttonRef.current) return;
    buttonRef.current = true;
    setTimeout(() => {
      setCount((prev) => prev + 1);
      buttonRef.current = false;
    }, 5000);
  };

  return (
    <div>
      <h1>Hello StackBlitz!</h1>
      <button onClick={handleClick}>Click Me</button>
      <p>{count}</p>
    </div>
  );
};

export default App;

EX-2 Debouncing- debouncing means like there is a input text box and when i write the text for searching john so api is called 4 times. and i want it will be called once when user stops typing
import React, { useState, useEffect } from 'react';
import './style.css';

const App = () => {
  const [search, setSearch] = useState('');

  useEffect(() => {
    const time = setTimeout(() => {
      console.log('api is calling');
    }, 5000);
    return () => clearTimeout(time);
  }, [search]);

  const handleChange = (e) => {
    const value = e.target.value;
    setSearch(value);
  };
  return (
    <div>
      <h1>Hello StackBlitz!</h1>
      <input type="text" placeholder="type here" onChange={handleChange} />
      <p>{search}</p>
    </div>
  );
};
export default App;

  
